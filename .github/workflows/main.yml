name: Clear Main Branch

on:
  workflow_dispatch:

jobs:
  clear-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch
        uses: actions/checkout@v3
        with:
          ref: main

      - name: Set Git user identity
        run: |
          git config --local user.email "kadineanthony9@gmail.com"
          git config --local user.name "kadine9"

      - name: Remove all files
        run: |
          git rm -rf .
          git clean -fd

      - name: Commit changes
        run: |
          git commit -m "Empty main branch"
        continue-on-error: true

      - name: Push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push origin main --force
